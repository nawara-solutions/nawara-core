{
  "variables": {
    "code": {
      "type": "code",
      "required": true,
      "secret": true,
      "maxLength": 12
    },
    "expiresAt": {
      "type": "datetime",
      "required": true
    }
  },
  "bodyText": "Your sign-in code is {{code}}. Valid until {{expiresAt}}. Never share it.",
  "smsMaxSegments": 2
}
