{
  "name": "nawara-core",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "prepare": "husky",
    "check:repo": "node scripts/check-repo.mjs",
    "test:repo": "node --test scripts/check-repo.test.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "yaml": "^2.9.1"
  }
}
